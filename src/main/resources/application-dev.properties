# =======================================================
# CONFIGURAÇÃO: DEV (Desenvolvimento Local)
# =======================================================

# Configurações do Banco de Dados DEV
spring.datasource.url=jdbc:postgresql://localhost:5432/dev
spring.datasource.username=postgres
spring.datasource.password=postgres

# Configuração DDL/Hibernate
# 'update': Hibernate atualiza o schema (adiciona colunas/tabelas) automaticamente.
# IDEAL para DEV, pois permite alterações rápidas no modelo.
spring.jpa.hibernate.ddl-auto=create-drop